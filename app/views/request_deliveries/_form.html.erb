<%= form_for(request_delivery) do |f| %>
  <% if request_delivery.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(request_delivery.errors.count, "error") %> prohibited this request_delivery from being saved:</h2>

      <ul>
      <% request_delivery.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
